# NEXT 5 STEPS

1. Run the first stock baseline using the provided manifest and fill all four baseline artifact files.
2. Lock a small fixed prompt set (short + long-context) and store prompt IDs in run notes.
3. Implement trace emission in baseline path using `docs/TRACE_SCHEMA.md` required fields.
4. Add a first Circular KV Lite toggle (off/on) without changing unrelated substrate behavior.
5. Execute A/B runs (stock vs Circular KV Lite) and record deltas in a new dated run folder.

