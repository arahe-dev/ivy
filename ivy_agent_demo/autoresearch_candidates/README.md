# AutoResearch Candidates

Each JSON file in this directory describes a candidate experiment.
The AutoResearch runner reads these specs and uses them to drive
bounded eval loops. Candidates should only target files allowed by
`ivy_agent_demo/autoresearch_config.json`.
